channels:
  - conda-forge
  - defaults

dependencies:
  - coveralls
  - cython
  - jsonschema
  - make
  - matplotlib
  - meson
  - moorpy=1.1.0
  - ninja
  - nlopt<=2.8
  - numpy<2.0
  - numpydoc
  - openmdao
  - openpyxl
  - pandas
  - pip
  - pydoe3
  - pyoptsparse
  - pytest
  - pytest-cov
  - python-benedict
  - pyyaml
  - ruamel.yaml
  - scipy
  - setuptools
  - simpy
  - sortedcontainers
  - statsmodels
  - swig
  - compilers  # provides gcc/g++/gfortran on Linux/macOS; required by setup.py
  - pip:
    - orbit-nrel>=1.2.1
    - dearpygui
# Windows-only extras (install separately if needed):
#  - m2w64-toolchain
#  - libpython
# Optional extras:
#  - petsc4py        # not on Windows
#  - mpi4py          # only if the system has MPI built and you want to use it
